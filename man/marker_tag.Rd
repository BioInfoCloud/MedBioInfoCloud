% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ImmuCellAI_mouse.R
\name{marker_tag}
\alias{marker_tag}
\title{Create Binary Marker Gene Tag Matrix}
\usage{
marker_tag(comgenes, tag_gene)
}
\arguments{
\item{comgenes}{Character vector of genes to include in the tag matrix (rows of the output).}

\item{tag_gene}{Character vector of marker genes for a specific cell type.}
}
\value{
Numeric vector (length = length(comgenes)) where 1 = gene is a marker, 0 = not a marker.
}
\description{
Internal helper function to generate a binary matrix indicating whether each gene is a marker for each cell type.
}
\examples{
# All genes in the expression matrix
all_genes <- c("Cd3e", "Cd4", "Cd19", "Cd8a", "Foxp3")

# Marker genes for T cells
tcell_markers <- c("Cd3e", "Cd4", "Cd8a")

# Create binary tag vector
tag_vec <- marker_tag(all_genes, tcell_markers)
}
