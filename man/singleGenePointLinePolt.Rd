% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geneExpVis.R, R/geneExpVisInTCGA.R
\name{singleGenePointLinePolt}
\alias{singleGenePointLinePolt}
\title{Plot Paired Gene Expression with Connecting Lines}
\usage{
singleGenePointLinePolt(
  data,
  cancer,
  gene,
  color = c("#E7B800", "#2E9FDF"),
  linecolor = "#1A1A1A"
)

singleGenePointLinePolt(
  data,
  cancer,
  gene,
  color = c("#E7B800", "#2E9FDF"),
  linecolor = "#1A1A1A"
)
}
\arguments{
\item{data}{A data frame containing paired gene expression data with required columns:
\code{gene} (gene names), \code{exp} (expression values), \code{Sample} (sample groups,
must include "Normal" and "Tumor"), \code{cancer} (cancer type), and \code{id} (patient identifiers)}

\item{cancer}{A character string specifying the cancer type to plot}

\item{gene}{A character string specifying the gene to plot}

\item{color}{A character vector of length 2 specifying colors for the two sample groups.
Defaults to c("#E7B800", "#2E9FDF")}

\item{linecolor}{A character string specifying the color for connecting lines.
Defaults to "#1A1A1A"}
}
\value{
A ggplot object showing paired expression values with connecting lines and statistical annotations.

A ggplot2 object
}
\description{
Generate a point plot with connecting lines for paired samples (e.g., matched normal-tumor) to visualize expression changes of a single gene in a specific cancer type, with paired t-test results.

This function creates a point-line plot to visualize paired expression data of a specific gene
in a specific cancer type (typically comparing normal and tumor samples from the same patients),
including paired t-test results.
}
\keyword{expression}
\keyword{paired}
\keyword{visualization}
